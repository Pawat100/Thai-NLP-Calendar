# Thai-NLP-Calendar
Convert natural  thai language into structured activities and display them on the calendar.
คู่มือการติดตั้งและใช้งานระบบ Thai NLP Calendar Chatbot แบบสมบูรณ์
ภาพรวม (Overview)

เว็บแอปนี้พัฒนาด้วย Streamlit และใช้ โมเดล spaCy NER ที่เทรนด้วยข้อมูลภาษาไทย เพื่อดึงข้อมูลเหตุการณ์ในปฏิทินจากข้อความภาษาธรรมชาติ (Natural Language)

ไฟล์ที่ประกอบในโปรเจกต์
ไฟล์หลักของแอปพลิเคชัน

app.py - ไฟล์หลักของ Streamlit Web Application

nlp_utils.py - เครื่องมือประมวลผล NLP (NER, แยกวันเวลา, Slot Mapping)

requirements.txt - รายการไลบรารี Python ที่ต้องติดตั้ง

ไฟล์สำหรับฝึกโมเดล (Training & Model)

train_model.py - สคริปต์สำหรับเทรนโมเดล spaCy NER

my_ner_model/ - โฟลเดอร์เก็บโมเดลที่เทรนแล้ว (จะถูกสร้างหลังรัน train_model.py)

เอกสารประกอบ

README.md - ไฟล์นี้

HOW_TO_RUN.txt - คู่มือเริ่มต้นใช้งานแบบย่อ

TROUBLESHOOTING.md - วิธีแก้ปัญหาที่พบบ่อย

install_dependencies.bat - ไฟล์ติดตั้งไลบรารีอัตโนมัติ (Windows)

RUN_WEBAPP.bat - ไฟล์เปิดเว็บแอปแบบคลิกเดียว (Windows)

🚀 เริ่มต้นใช้งานอย่างรวดเร็ว (3 ขั้นตอน)
ขั้นตอนที่ 1: ติดตั้ง Dependencies
pip install -r requirements.txt


หรือดับเบิลคลิกไฟล์: install_dependencies.bat

ขั้นตอนที่ 2: เทรนโมเดล spaCy
python train_model.py


ระบบจะ:

สร้างโมเดล spaCy เปล่า

เทรนด้วยข้อมูล NER ภาษาไทยจาก NLP_PROJECT.ipynb

บันทึกโมเดลไว้ในโฟลเดอร์ my_ner_model/

ใช้เวลาประมาณ 1–2 นาที

ขั้นตอนที่ 3: รัน Web Application
streamlit run app.py


หรือดับเบิลคลิกไฟล์: RUN_WEBAPP.bat

📊 โครงสร้างการทำงานของระบบ (Architecture)
ลำดับการไหลของข้อมูล (Data Flow)
ข้อความผู้ใช้ (ภาษาไทย)
    ↓
ปรับข้อความให้อยู่ในรูปแบบมาตรฐาน (Text Normalization)
    ↓
โมเดล spaCy NER
    ↓
ดึง Entity (DATE, TIME, ACTIVITY, PERSON, LOCATION)
    ↓
ตรวจสอบด้วย POS Tagging
    ↓
Slot Mapping
    ↓
แปลงวันเวลา (Date/Time Parsing)
    ↓
สร้างและบันทึก Event (events.json)
    ↓
แสดงผลบนปฏิทิน UI

องค์ประกอบสำคัญของระบบ
1. โมเดล spaCy NER (my_ner_model/)

เทรนจาก TRAIN_DATA ใน NLP_PROJECT.ipynb

จำแนก Entity ได้ 6 ประเภท:

ACTIVITY

DATE

TIME

END_TIME

LOCATION

PERSON

ใช้ multilingual blank model เป็นฐาน

2. การปรับข้อความภาษาไทย (nlp_utils.py)

ปรับ Unicode

ลบช่องว่างเกิน

ใช้ไลบรารี pythainlp

3. ตัวแปลงวันเวลา (nlp_utils.py)

รองรับตัวอย่างเช่น:

"พรุ่งนี้" → "2026-02-10"

"วันจันทร์" → วันจันทร์ถัดไป

"10 กุมภาพันธ์" → "2026-02-10"

"บ่าย 3 โมง" → "15:00"

4. Slot Mapping (nlp_utils.py)

แปลง Entity เป็นโครงสร้าง Event

ตรวจสอบความถูกต้องด้วย POS Tag

สร้าง Object เหตุการณ์แบบมีโครงสร้าง

5. การจัดเก็บข้อมูลเหตุการณ์ (events.json)

ไฟล์ JSON สำหรับเก็บ Event ทั้งหมด

สร้างอัตโนมัติเมื่อรันเว็บแอป

รองรับ Export / Import

🎓 แหล่งข้อมูลสำหรับการเทรน (Training Data Source)

ข้อมูล TRAIN_DATA มาจากไฟล์ NLP_PROJECT.ipynb

บรรทัด 2408–2700+ เป็นตัวอย่างเหตุการณ์ปฏิทินภาษาไทย

ใช้รูปแบบ Weak Annotation สำหรับ spaCy

ตัวอย่างข้อมูลเทรน
("พรุ่งนี้มีประชุมตอน 10 โมง", 
 {"entities": [(0, 8, "DATE"), (10, 16, "ACTIVITY"), (20, 26, "TIME")]})
 
("วันจันทร์นัดหมอ 3 โมง", 
 {"entities": [(0, 9, "DATE"), (9, 12, "ACTIVITY"), (12, 15, "PERSON"), (16, 22, "TIME")]})

📝 เกี่ยวกับโมเดล NER
เหตุผลที่เลือก spaCy (ไม่ใช้ WangchanBERTa)

WangchanBERTa ไม่สำเร็จ – ไฟล์ train.json/test.json มาจากการทดลองที่ล้มเหลว

spaCy ใช้ง่ายกว่า

เพียงพอสำหรับงานปฏิทิน

ประมวลผลเร็วกว่า BERT

ประเภท Entity
Label	คำอธิบาย	ตัวอย่างภาษาไทย	ตัวอย่างอังกฤษ
DATE	วันที่	พรุ่งนี้, วันจันทร์	tomorrow
TIME	เวลา	10 โมง, บ่าย 3 โมง	3pm
END_TIME	เวลาสิ้นสุด	ถึง 5 โมง	until 5pm
ACTIVITY	กิจกรรม	ประชุม, สอบ	meeting
PERSON	บุคคล	บีม, อาจารย์วิชัย	Beam
LOCATION	สถานที่	ห้อง 9906	hospital
🔄 การปรับแต่งระบบ (Customization)
เพิ่มข้อมูลเทรน

เปิด train_model.py

เพิ่มข้อมูลใน TRAIN_DATA

รันใหม่ python train_model.py

ปรับพารามิเตอร์การเทรน
model = train_ner_model(
    output_dir="my_ner_model",
    n_iter=30
)


เพิ่ม n_iter จะทำให้แม่นขึ้นแต่ช้าลง

🐛 ปัญหาที่พบบ่อย
"Model not found"

ปกติในการรันครั้งแรก
ให้รัน python train_model.py ก่อน

ความแม่นยำต่ำ

เพิ่มตัวอย่างเทรน

เพิ่ม n_iter

ตรวจ boundary ของ entity

ตรวจจับ Entity ไม่ได้

ตรวจว่ามีตัวอย่างคล้ายใน TRAIN_DATA หรือไม่

เพิ่ม edge case

ตรวจ normalization

📚 ไฟล์ที่เกี่ยวข้อง (โฟลเดอร์แม่)

NLP_PROJECT.ipynb

THENLP.ipynb

nlp_utils.py

🎯 ตัวอย่างการใช้งาน

Input

พรุ่งนี้มีประชุมกับบีมตอน 10 โมง


Output

{
  "date": "2026-02-10",
  "time": "10:00",
  "description": "ประชุม",
  "attendees": "บีม",
  "location": null
}

🚀 การ Deploy
Local
streamlit run app.py

Production

Streamlit Cloud

Push GitHub

เชื่อม https://streamlit.io/cloud

Deploy

Docker

FROM python:3.9
COPY . /app
WORKDIR /app
RUN pip install -r requirements.txt
CMD ["streamlit", "run", "app.py"]

📞 การช่วยเหลือ

ปัญหาเทรน → train_model.py

ปัญหาไลบรารี → install_dependencies.bat

Runtime Error → TROUBLESHOOTING.md

ความแม่นยำ → เพิ่ม Training Data

🔗 แนวคิดสำคัญ

NER – การระบุชื่อเฉพาะ

POS Tagging – ตรวจชนิดคำ

Slot Mapping – แปลงเป็นโครงสร้างข้อมูล

Weak Annotation – การติดป้ายแบบง่าย
